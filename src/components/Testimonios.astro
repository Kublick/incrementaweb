---
import { testimoniosData } from "../data/testimoniosData";
import Card from "./Card.astro";
import CTABoton from "./CTABoton";
---

<section class="py-12">
  <h1 class="text-2xl md:text-4xl w-3/4 mx-auto text-center mb-8">
    ¿Se puede multiplicar el número de consultas en pocos días? Ellos lograron
    eso y mucho más
  </h1>
  <div
    class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 mx-2 md:mx-8 place-items-center"
  >
    {
      testimoniosData.map((testimonio) => (
        <Card
          nombre={testimonio.nombre}
          contenido={testimonio.contenido}
          img={testimonio.img}
          titulo={testimonio.titulo}
        />
      ))
    }
  </div>

  <div class="py-12">
    <CTABoton client:visible />
  </div>
</section>
