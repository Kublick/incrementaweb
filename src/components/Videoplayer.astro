---
type Props = {
  source: string;
  width?: number;
  height?: number;
};

import VP from "./vp";

const { width = 640, height = 360, source } = Astro.props as Props;
---

<VP
  width={width}
  height={height}
  url={`https://www.youtube.com/watch?v=${source}`}
  client:load
/>
