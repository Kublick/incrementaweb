<style>
  .stuff {
    opacity: 0;
    filter: blur(5px);
    transform: translateX(-100%);
    transition: all 1s;
  }

  @media (prefers-reduced-motion) {
    .stuff {
      transition: none;
    }
  }
  .show {
    opacity: 1;
    filter: blur(0);
    transform: translateX(0);
  }
  .logo {
    background-color: transparent;
  }

  .logo:nth-child(2) {
    transition: 200ms;
  }
  .logo:nth-child(3) {
    transition: 400ms;
  }
  .logo:nth-child(4) {
    transition: 600ms;
  }
</style>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("show");
      } else {
        entry.target.classList.remove("show");
      }
    });
  });

  const hiddenElements = document.querySelectorAll(".stuff");

  hiddenElements.forEach((el) => observer.observe(el));
</script>

<section class="h-full bg-lightPurple bg-cover pb-12">
  <div class="grid gap-4 p-12 text-white md:grid-cols-2">
    <div class="place-self-center">
      <img
        src="Berenice_2.webp"
        alt="Berenice Bastidas"
        width="527"
        loading="lazy"
        class="rounded-lg"
      />
    </div>
    <div class="place-self-center">
      <h1 class="font-bold md:text-4xl">
        Hola, soy <span class="text-3xl font-bold">Berenice</span>
      </h1>
      <p class="stuff icon mt-4 pb-4 text-xl md:text-lg md:tracking-wide">
        Ayudo a médicos, dentistas, psicólogos, nutriólogos, terapeutas y otros
        profesionales que se dedican a la consulta privada particular a
        incrementar su número de pacientes con estrategia a través de internet,
        incluso si van empezando desde cero y se sienten poco tecnológicos. Cree
        mi propia metodología de prospección de pacientes, he podido compartir
        este conocimiento con otros profesionales de la salud, que se dedican a
        la consulta privada, impactando de forma positiva en la vida de más de
        200 alumnos que han pasado ya por mis formaciones y asesorías, teniendo
        presencia ya en 14 países de Latinoamérica, con mi programa avanzado el
        METODO INCREMENTA TU CONSULTA. Sabías que hoy en día tu puedes lograr
        generar miles de dólares en ventas de tu consulta privada usando
        internet, haciéndolo de forma fácil, generando tú mismo tu propia
        publicidad en las redes sociales, ahorrándote con esto lo que te
        cobraría una agencia de marketing y publicidad..
      </p>
    </div>
  </div>
</section>
