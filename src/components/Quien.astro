<style>
  .stuff {
    opacity: 0;
    filter: blur(5px);
    transform: translateX(-100%);
    transition: all 1s;
  }

  @media (prefers-reduced-motion) {
    .stuff {
      transition: none;
    }
  }
  .show {
    opacity: 1;
    filter: blur(0);
    transform: translateX(0);
  }
  .logo {
    background-color: transparent;
  }

  .logo:nth-child(2) {
    transition: 200ms;
  }
  .logo:nth-child(3) {
    transition: 400ms;
  }
  .logo:nth-child(4) {
    transition: 600ms;
  }
</style>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("show");
      } else {
        entry.target.classList.remove("show");
      }
    });
  });

  const hiddenElements = document.querySelectorAll(".stuff");

  hiddenElements.forEach((el) => observer.observe(el));
</script>

<section
  class="h-full bg-secondary bg-[url(/fondo2_berenice.png)] bg-cover pb-12"
>
  <div class="grid gap-4 p-12 text-white md:grid-cols-2">
    <div class="place-self-center">
      <img
        src="Berenice_2.webp"
        alt="Berenice Bastidas"
        width="527"
        loading="lazy"
        class="rounded-lg"
      />
    </div>
    <div class="place-self-center">
      <h1 class="font-bold md:text-4xl">
        Hola, soy <span class="text-3xl font-bold">Berenice</span>
      </h1>
      <p
        class="stuff icon mt-4 pb-4 text-xl md:text-xl md:tracking-wide lg:w-1/2"
      >
        Soy Psicóloga Berenice Bastidas y descifré como llenar mi agenda medio
        tiempo, de pacientes durante 3 años seguidos y esto fue antes de
        dedicarme a ayudar a Nutriólogos, Psicólogos, Terapeutas,
        Psicoterapeutas y Profesionales de la Salud a poder hacerlo para ellos
        mismos multiplicando sus ingresos e incrementando su número de pacientes
        y cuando empiezan desde cero con lo que aprenden en el Entrenamiento
        Incrementa Tu Consulta, logran su primera venta de consulta en 40 días o
        menos.
      </p>
    </div>
  </div>
</section>
